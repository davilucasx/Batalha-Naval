#ifndef _LEITORTXT_H//if not defined 
#define _LEITORTXT_H

void leitor_texto(const char* nomearquivo);//Declaracao de uma funcao chamada leitor_texto que recebe um
// ponteiro para caractere constante (const char*) como argumento e nao retorna nenhum valor (void).

#endif 